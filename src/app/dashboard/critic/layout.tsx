import CriticLayoutWrapper from '@/components/dashboard/critic/layout';

const CriticLayout = ({ children }: { children: React.ReactNode }) => {
  return <CriticLayoutWrapper>{children}</CriticLayoutWrapper>;
};

export default CriticLayout;
